#!/bin/bash

COMPILER_DIR=@COMPILER_DIR@
COMPILER_BINDIR=${COMPILER_DIR}/@CT_PREFIX@/bin
BIN_NAME=$(basename ${0})

local LDPARAM=""
[[ ${BIN_NAME/@TUPLES@-/} == "ld" ]] && LDPARAM=-EL

${COMPILER_BINDIR}/${BIN_NAME/@TUPLES@-/} ${LDPARAM} "$@"


