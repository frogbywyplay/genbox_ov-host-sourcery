#!/bin/bash

COMPILER_DIR=@COMPILER_DIR@
COMPILER_BINDIR=${COMPILER_DIR}/@CT_PREFIX@/bin
BIN_NAME=$(basename ${0})

LDPRM=""
[[ ${BIN_NAME/@TUPLES@-/} == "ld" ]] && LDPRM=-EL

${COMPILER_BINDIR}/${BIN_NAME/@TUPLES@-/} ${LDPRM} "$@"


